# Foreman remote execution: read the rex key from /usr (immutable, survives the
# /var first-boot reset that drops %post-injected keys). Still honors per-user
# ~/.ssh/authorized_keys for anything else.
AuthorizedKeysFile /usr/share/foreman-rex/%u.keys .ssh/authorized_keys
